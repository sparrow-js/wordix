export const BASE_BUILTIN_MODULES = [
  "math",
  "random",
  "datetime",
  "json",
  "re",
  // Add other allowed modules
];

export const BASE_PYTHON_TOOLS = {
  // Add base Python tools here
};

export function evaluatePythonCode(code: string, options: any): [any, string, boolean] {
  // Implementation to be added
  return [null, "", false];
} 